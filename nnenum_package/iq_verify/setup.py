from setuptools import setup, find_packages

setup(
    name="iq_verify",
    version="0.0.0a",
    packages=find_packages(),
    install_requires=[
        # List dependencies here, e.g.,
        # "numpy",
    ],
    python_requires=">=3.6",
)
